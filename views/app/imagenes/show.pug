extends ../../layout.pug
block contenido
	div(class="img-container")
		h1 Título #{imagen.title}
		img(src="/estatico/images/"+imagen.id+"."+imagen.extension class="main-img")
		if(typeof imagen.creator != "undefined")
			p Creador por #{imagen.creator.email}
				