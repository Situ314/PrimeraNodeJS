extends ./layout.pug
block contenido
	div(class="col-md-5 remove-float center-block big-top-space")
		h1(class="text-center") Login de Usuario
		form(action="/sessions" method="POST" class="top-space")
			div(class="form-group")
				label(for="email") Email
				input(type="text" name="email" placeholder="Email" id="email" class="form-control")
			div(class="form-group")
				label(for="password") Password
				input(type="password" name="password" placeholder="Password" id="password" class="form-control")
			div(class="row top-space")
				div(class="col-xs-12 col-sm-6")
					input(type="submit" class="btn btn-info" value="Inciar sesión")
				div(class="col-xs-12 col-sm-6 text-right")
					a(href="/signup") No tengo cuenta